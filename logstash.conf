input {
  jdbc {
    jdbc_driver_library => "/usr/share/logstash/logstash-core/lib/jars/mssql-jdbc-13.4.0.jre11.jar"
    jdbc_driver_class => "com.microsoft.sqlserver.jdbc.SQLServerDriver"
    jdbc_connection_string => "jdbc:sqlserver://host.docker.internal:1433;databaseName=thuoc;encrypt=true;trustServerCertificate=true"
    jdbc_user => "sa"
    jdbc_password => "123456"
    statement => "SELECT * FROM dbo.danh_sach_thuoc"
    jdbc_paging_enabled => true
    jdbc_page_size => 5000
  }
}
filter {
  mutate {
    remove_field => ["@version", "@timestamp"]
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "danh_sach_thuoc"
    document_id => "%{sku}"
  }
  stdout { codec => rubydebug }
}